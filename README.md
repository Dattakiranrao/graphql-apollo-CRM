# Client Relationship Management App (CRM)

Client Relationship Management app using React, Express, GraphQL & Apollo
## Usage

### Install

1. Install Docker for your operating system.
2. Install the [Docker Compose](https://docs.docker.com/compose/install/) tool.
3. Run docker-compose

```
docker-compose up -d --build
```

This will start backend, frontend and MongoDB.
